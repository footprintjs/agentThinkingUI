node_modules/
dist/
coverage/
*.log
.DS_Store
.vitest/

# scratch / session artifacts (not part of the library)
scraps/
uploads/
.thumbnail
*.napkin

# Published library: lockfile is dev-only (stripped on publish) and a committed cross-OS
# lock can break CI; resolve fresh.
package-lock.json
